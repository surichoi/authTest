spring:
  datasource:
    url: jdbc:h2:~/data/auth
    username: suri
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true